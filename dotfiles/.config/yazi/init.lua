require("full-border"):setup()

-- function Linemode:size_and_permissions()
-- 	local size = self._file:size()
-- 	local permissions = self._file:permissions()
-- 	return string.format("%s %s", size and ya.readable_size(size) or "-", permissions)
-- end
